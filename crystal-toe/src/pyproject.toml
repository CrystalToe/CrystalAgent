[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "crystal-toe"
version = "0.1.0"
description = "Crystal Topos — Physics from A_F = ℂ ⊕ M₂(ℂ) ⊕ M₃(ℂ)"
license = {text = "AGPL-3.0-or-later"}
requires-python = ">=3.8"

[tool.maturin]
features = ["python"]
